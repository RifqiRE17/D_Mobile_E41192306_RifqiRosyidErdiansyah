<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tugas.aksessensor">

    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AksesSensor">
        <activity android:name="com.tugas.aksessensor.ProximitySensorActivity"></activity>
        <activity android:name="com.tugas.aksessensor.LightSensorActivity" />
        <activity android:name="com.tugas.aksessensor.AllSensorActivity" />
        <activity android:name="com.tugas.aksessensor.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


    </application>

</manifest>